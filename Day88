CREATE TABLE bank_account (
    account_no INT PRIMARY KEY,
    account_holder VARCHAR(50),
    balance DECIMAL(10,2)
);

INSERT INTO bank_account VALUES
(101, 'Arun', 5000),
(102, 'Bala', 8000),
(103, 'Charan', 10000);

START TRANSACTION;

UPDATE bank_account
SET balance = balance - 1000
WHERE account_no = 101;

UPDATE bank_account
SET balance = balance + 1000
WHERE account_no = 102;

-- Undo the transaction
ROLLBACK;

SELECT * FROM bank_account;
CREATE TABLE employee (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50),
    salary DECIMAL(10,2),
    department VARCHAR(30)
);

INSERT INTO employee VALUES
(1,'Ravi',30000,'IT'),
(2,'Kumar',25000,'HR'),
(3,'Anu',40000,'Finance');

CREATE VIEW employee_names AS
SELECT emp_name
FROM employee;

CREATE USER 'guest'@'localhost' IDENTIFIED BY 'guest123';

GRANT SELECT ON employee_names TO 'guest'@'localhost';

SELECT * FROM employee_names;
CREATE TABLE employee (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50),
    salary INT
);

CREATE DATABASE transport_company;

USE transport_company;


CREATE TABLE vehicle_details (
    vehicle_id INT PRIMARY KEY,
    vehicle_name VARCHAR(50),
    vehicle_number VARCHAR(20),
    capacity INT
);


CREATE TABLE delivery_details (
    delivery_id INT PRIMARY KEY,
    product_name VARCHAR(50),
    shop_name VARCHAR(50),
    delivery_area VARCHAR(50),
    status VARCHAR(20)
);
START TRANSACTION;

INSERT INTO delivery_details VALUES
(1,'Laptop','ABC Shop','Chennai','Completed'),
(2,'Mobile','XYZ Shop','Madurai','Pending'),
(3,'TV','Home Needs','Coimbatore','Completed');

COMMIT;


SELECT * FROM delivery_details;
START TRANSACTION;

DELETE FROM delivery_details
WHERE delivery_id = 2;

ROLLBACK;


SELECT * FROM delivery_details;
